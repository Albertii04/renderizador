{
  "name": "@renderizador/utils",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@renderizador/config": "workspace:*",
    "@renderizador/types": "workspace:*"
  },
  "devDependencies": {
    "@renderizador/typescript-config": "workspace:*",
    "vitest": "^3.1.3"
  }
}
