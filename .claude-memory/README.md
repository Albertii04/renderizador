# Claude memory snapshot

Mirror of `~/.claude/projects/-Users-albertmp-Developer-renderizador/memory/`.

To sync on another device:

```bash
mkdir -p ~/.claude/projects/-Users-albertmp-Developer-renderizador/memory
cp .claude-memory/*.md ~/.claude/projects/-Users-albertmp-Developer-renderizador/memory/
```

Refresh before commit:

```bash
cp ~/.claude/projects/-Users-albertmp-Developer-renderizador/memory/*.md .claude-memory/
```
